import React, { Component } from 'react';

class ProductDetails extends Component {
  componentDidMount() {
    const { match } = this.props;
  }

  render() {
    return (
      <div>ProducDetails</div>
    );
  }
}

export default ProductDetails;
